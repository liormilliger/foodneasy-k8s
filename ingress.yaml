apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: foodneasy-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: foodneasy.example.com  # Replace with your domain
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: foodneasy-app
            port:
              number: 80